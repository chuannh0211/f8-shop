1. Làm quen với css
    - Các cách sử dụng css trong html:
    + Internal (nội bộ): đặt trong thẻ style, có thể đặt ở mọi nơi trong file html
    + External (ngoại tuyến): sử dụng file từ bên ngoài link vào file html
    + Inline(nội tuyến): dùng thuộc tính style bên trong thẻ khác
2. CSS selector
Selector	Ví dụ	Mô tả
.class	| .intro	| Chọn tất cả các thẻ có class=“intro”
.class1.class2	| .name1.name2	| Chọn tất cả các thẻ có cả name1 và name2 được đặt trong thuộc tính class của nó
.class1 .class2	| .name1 .name2	| Chọn tất cả các thẻ có class=“name2” là con của một phần tử có class=“name1”
*	| *	| Chọn tất cả các thẻ
element	| h2	| Chọn tất cả các thẻ h2
element.class	| div.box	| Chọn tất cả thẻ div có class=“box”
element, element	| div, h2	| Chọn tất cả thẻ div và h2
element element	| div p	| Chọn tất cả thẻ p trong thẻ div
element > element	| div > p	| Chọn tất cả thẻ p là con trực tiếp của thẻ div
element + element	| div + p	| Chọn thẻ p đứng liền kề sau thẻ div
element ~ element	| div ~ p	| Chọn tất cả thẻ p đứng sau thẻ div
3. Độ ưu tiên trong css
    - Internal và External có cùng mức độ ưu tiên (cái nào viết trc thì sẽ đc load trước)
    - tag(tên element) < .class < #id < Inline
4. Đặt biến trong CSS
    - Sử dụng trong trường hợp có nhiều element có dùng chung 1 thuộc tính nào đó
    EX: 
    :root{
        --text-color: violet;
    }
    h1{
        color: var(--text-color);
    }
    h2{
        color: var(--text-color);
    }
    h3{
        color: var(--text-color);
    }
5. CSS pseudo-classes(Lớp giả)
    - :root
    - :hover: di chuột vào phần tử 
    - :active: click chuột vào phần tử
    - :first-child: lấy phần tử con đầu tiền
    - :last-child: lấy phần tử con cuối cùng
6. CSS pseudo-element
    - tạo ra 1 element mà k cần viết mã html mà chỉ cần viết css
7. Padding (đệm)
    -> note: thêm thuộc tính padding có thể làm tăng kích thước của element
8. Border (viền)
    - border-witdh: độ rộng của border
    - border-style: kiểu border
    - border-color: màu border
    - shorthand: border: độ rộng | kiểu border | màu sắc  
    -> note: thêm thuộc tính border có thể làm tăng kích thước của element
9. Margin (khoảng lề)
10. Box-sizing: 
    - tự động tính toán chiều ngang element = tổng kích thước của border + padding + content ( trừ trường hợp đặt padding or border or margin 
    quá lớn so với element thì k giữ nguyên được)
11. Background-image:
    - linear-gradient(hướng xoay, mã màu, mã màu): màu chuyển
    - màu chuyển trong suốt -> linear-gradient(hướng xoay, rgba(), rgba());
12. CSS Position: Relative (vị trí tương đối)
    - Không phụ thuộc vào đối tượng nào khác, mà lấy chính bản thân nó làm gốc tọa độ và chỉ phụ thuộc vào chính nó
    - attribute: top, left, right, bottom
13. CSS Position: Absolute (vị trí tuyệt đối)
    - Sử dụng khi element cần set thuộc tính position phụ thuộc vào 1 element khác 
    - phụ thuộc vào thẻ cha gần nhất có thuộc tính position -> lấy thẻ cha đó làm gốc tọa độ
14. CSS Position Fix
    - Sử dụng khi element cần set thuộc tính position phụ thuộc vào trình duyệt
15. Những thành phần thường gặp trên giao diện web
    - Header
    - Navigation (điều hướng)
    - Breadcrum 
    - Sidebar
    - Slider
    - Content
    - Footer